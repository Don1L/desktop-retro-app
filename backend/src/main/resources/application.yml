spring:
  datasource:
    url: jdbc:postgresql://dpg-d1gfom21i9vc73ajmpdg-a:5432/desktop_db
    username: desktop_db_user
    password: 0QdOAm2AwtlEdgq1bxiMk0Z1DukfVKSR
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  # По желанию: логгирование SQL-запросов
  jpa:
    show-sql: true

server:
  port: 8080
